profile::pybal::config_host: conf2006.codfw.wmnet
profile::pybal::etcd_port: 4001
profile::lvs::tagged_subnets:
  - public1-eqsin

profile::lvs::interface_tweaks:
  enp94s0f0np0:
    txqlen: 10000

profile::lvs::ipip_enabled: true
profile::pybal::ipip_enabled: true

profile::lvs::do_ipv6_ra_primary: true  # temporary override till we roll out T358260
