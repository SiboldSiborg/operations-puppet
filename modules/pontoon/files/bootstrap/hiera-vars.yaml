# SPDX-License-Identifier: Apache-2.0

# Variables overridden during role::puppetserver::pontoon bootstrap.

# Environment available:
# - FQDN

# profile::puppet::agent depends on this to bootstrap
puppetmaster: ${FQDN}
puppet_ca_server: ${FQDN}

profile::puppetserver::ca_server: ${FQDN}
profile::puppet::agent::puppetmaster: ${FQDN}
